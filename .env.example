# Vercel production and `vercel dev` use same-origin /api routes, so keep this empty.
# When running standalone `npm run dev`, copy this file to .env.local and replace
# the value with a deployed Vercel Preview/Production origin, without a trailing slash.
VITE_API_BASE=
