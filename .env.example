# Vercel production uses same-origin /api routes, so this can stay unset there.
# When Vite runs on :5173 and `vercel dev` serves Functions on :3000,
# copy this file to .env.local and keep the local API origin below.
VITE_API_BASE=http://localhost:3000
